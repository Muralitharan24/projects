import { PositionpipePipe } from './positionpipe.pipe';

describe('PositionpipePipe', () => {
  it('create an instance', () => {
    const pipe = new PositionpipePipe();
    expect(pipe).toBeTruthy();
  });
});
